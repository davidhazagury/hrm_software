<%= simple_form_for [:administrator,@alert] do |f| %>
  <%= f.input :notification, required:true %>
  <%= f.input :description, required: true %>
  <%= f.submit t('components.buttons.update') %>
<% end %>
