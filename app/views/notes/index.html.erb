Notas de: <%= @worker.first_name %>

<ol>
  <% @notes.each do |n| %>
    <li>
        <ul>
          <li>C<%= n.content %>
            <% if policy(Note).destroy? %>
              <%= link_to "Eliminar", note_path(n), method: :delete, data: {confirm:'Estas seguro?'}  %>
            <% end %>
            </li>

        </ul>
    </li>
  <% end %>
</ol>
